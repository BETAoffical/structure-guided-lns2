# 稳定4车候选的延续结果：一步下降未转化为恢复

## 结论

16/16诊断正常完成，各64步，共1024次修复。零运行错误、墙钟删失、原生超时或路径核验错误。原策略与替代首步均0/8可行，结论为本轮机制门槛未通过。

JSON统一decision为`no_go_or_inconclusive`，其中`no_budget_censoring=true`。本次应解释为`NO_GO`，不是预算未知；也不证明更长延续必然无解。

预登记提交`c5561c29a68ad60a68f2e2a953f87183b87b875a`，在运行前已推送。恢复标签`pre-stable-candidate-continuation-20260910`指向`3c4183f95a0a9be3d2b3bfa7c2e0ce1dc2ae0637`。没有更换候选、追加trials、改变门槛或执行正式TTF。

## 结果

状态`case-3723c8bd272e`，初始40对冲突。替代候选`neighborhood-0d3103de10e25097`，车辆`[54,222,245,287]`。两分支每trial首步PP seed配对；首步以后均复用冻结Dual16控制器。

| 指标 | 原Dual16 | 指定4车首步后恢复Dual16 |
|---|---:|---:|
| 可行数 | 0/8 | 0/8 |
| 每trial首步后冲突 | 40 | 39 |
| 每trial第64步后冲突 | 40 | 39 |
| 首步后严格下降次数合计 | 0 | 0 |
| 回滚次数合计 | 512 | 504 |
| 固定64步冲突AUC均值 | 2560 | 2496.5 |
| 每trial PP秒均值，并发诊断 | 16.78749 | 16.90516 |

八组trial完全一致地表现为原策略`40,...,40`、替代分支`40,39,...,39`。替代分支的AUC降低2.48046875%，全部由第一步少一对冲突造成，不能称为持续恢复、任务成功或TTF加速。

每项实际延续墙钟约26至29秒，均先达到64步上限而非60秒上限。并发计时只用于记录开销，不作速度比较。没有得到可行解，故不报告成功TTF。

## 原因定位

从保存的初始和最终冲突边重新比较，八个替代分支都只移除了`(222,245)`，未增加或删除其他冲突边。该车对在初始冲突图中构成一个仅含两节点的连通分量；四辆被选车辆关联的初始冲突边也只有这一条。

恢复原策略后，每trial余下63次修复全部回滚，实际PP顺序各不相同。每trial其中61至63次重新选择原困难16车集合，余下为一个相近的16车集合。这次不是随机seed相同、动作没有应用或控制器没有恢复。

观察支持：这个稳定的一步候选解决了当前图中独立的一对冲突，却没有在随后原策略延续中改善剩余困难状态。冲突图当下断开不代表路径搜索在所有未来选择下都独立，不能据此证明该候选永远没有价值或全部两车协调方法无效。

这也说明历史8/8一步下降并不等于8/8脱困。不能因为首步标签稳定，就把它当成长期救援训练标签。

## 验证范围

- 新入口7项定向单元测试通过；原延续器255项文件绑定通过后才复用。
- 每项重放44个历史动作，验证完整fingerprint和PP顺序；源候选池、成员、分数和原选择复现。
- 核对16项结果、首步候选、配对seed、后续冻结排名最大分数选择、显式成员、路径修改归属、trace链、终态fingerprint、路径端点/移动/SOC及重建冲突边。
- AUC由保存冲突序列重新计算，结果和路径SHA封存，运行器结束时active为0，锁已释放。
- 未改正式控制器、模型、PP/native，也未重编译或重跑全量Python/CTest/正式实验。测试声明仅限本次定向验证，不冒称全套回归。
- 一个事后选定状态、八条新随机流，不能作为总体成功率或独立跨地图确认。

完整结果：`build/initlns-stable-candidate-continuation-v1/report.json`，SHA256为`e35a116a0551b2d873e2bcd867a9117d6782825856ffded4c9f68881a1f25d11`。预注册计划与种子见`artifacts/initlns-stable-candidate-continuation-v1/registration.json`。

## 下一步边界

按预登记停止，不继续枚举候选，不增加步数/预算，不训练救援排序器或RL，不上线首步覆盖，也不启动计时。Official、V2、Dual16保持冻结。

已有同状态选择机会并不都能转化为持续进展。这条稳定单候选首步恢复分支到此结束；若未来继续研究进入困难状态之前的决策，必须另立同状态证据与预算，不能把本次NO_GO改写为支持扩采。
